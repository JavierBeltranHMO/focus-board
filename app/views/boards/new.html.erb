<h1>New Board</h1>
<%= form_with model: @board, local: true do |form| %>
  <div class="mb-3">
    <%= form.label :name, class:"form-label" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>
  <%= form.submit "Create Board", class:"btn btn-success" %>
<% end %>